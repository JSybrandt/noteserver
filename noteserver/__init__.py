"""An example module."""


def is_less_than_10(var: int) -> bool:
  """An example function.

  Args:
    var: A value that may-or-may-not be less than 10.

  Returns:
    True if var is less than 10.
  """
  return var < 10
